{
  "name": "msx-to-luau",
  "version": "1.0.0",
  "description": "Convert MSX Scripts to Luau, It converts MSRAW -> Luau",
  "scripts": {
    "build": "tsc convert.ts",
    "pkg": "pkg convert.js -t node20-win-x64,node20-linux-x64,node20-macos-x64 -o convert.exe"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "readline-sync": "^1.4.10"
  },
  "devDependencies": {
    "@types/node": "^20.10.6"
  }
}
